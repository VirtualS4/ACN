<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="main.acn">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LelangDetail"></activity>
        <activity android:name=".LelangActivity" />
        <activity android:name=".ManageActivity" />
        <activity android:name=".PetugasDetail" />
        <activity android:name=".MasyarakatDetail" />
        <activity android:name=".BarangDetail" />
        <activity android:name=".RekapDetail" />
        <activity android:name=".Rekap" />
        <activity android:name=".ManageMenu" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>